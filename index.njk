---
title: home
type: pages
order: 1
---

<div class="banner">
  <img src="/assets/images/I need to know your form.jpg" alt="Welcome to the Void Femmes website">
</div>

<h1>Welcome to Void Femmes</h1>
<p>An electronic music project by Lex Feathers. This site acts as a directory of updates and as a scratchpad. Please enjoy your stay.</p>

<h1 class="faded" style="margin-bottom: 0;">Latest:</h1>

<article class="homepage-excerpt">
  {# Get the latest post #}
  {% set latestPost = search.pages("type=posts draft=false", "published=desc", 1) %}
  {% asyncEach post in latestPost %}
  <div class="homepage-excerpt-container">
    <div class="margin-bar"></div>
    <div>
      <h3>{{ post.title }}</h3>

      <a href="{{ post.url }}">
        <span class="homepage-excerpt-date">
          Posted {% include "/templates/date.njk" %} by {{ post.author }}
        </span>
      </a>
      <figure>
        {% ifAsync post.excerpt %}
        {{ post.excerpt | safe }}
        {% else %}
        {{ post.content | safe }}
        {% endif %}
      </figure>
    </div>
  </div>

  <a href="{{ post.url }}" class="button read-more" style="text-align: right;"><span>Read more -></span></a>
  {% endeach %}
</article>
